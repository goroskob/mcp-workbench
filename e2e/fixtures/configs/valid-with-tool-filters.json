{
  "toolboxes": {
    "test": {
      "description": "Test toolbox with tool filters",
      "mcpServers": {
        "memory": {
          "command": "npx",
          "args": ["-y", "@modelcontextprotocol/server-memory"],
          "toolFilters": ["store_value", "get_value"]
        }
      }
    }
  }
}
