{
  "toolMode": "proxy",
  "toolboxes": {
    "test-memory": {
      "description": "Test toolbox with memory server for proxy mode testing",
      "mcpServers": {
        "memory": {
          "command": "npx",
          "args": ["-y", "@modelcontextprotocol/server-memory"],
          "toolFilters": ["*"],
          "transport": "stdio"
        }
      }
    }
  }
}
