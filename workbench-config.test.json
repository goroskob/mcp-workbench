{
  "toolboxes": {
    "memory-test": {
      "description": "Simple test toolbox using the memory MCP server",
      "mcpServers": {
        "memory": {
          "command": "npx",
          "args": ["-y", "@modelcontextprotocol/server-memory"],
          "toolFilters": ["*"],
          "transport": "stdio"
        }
      }
    }
  }
}
